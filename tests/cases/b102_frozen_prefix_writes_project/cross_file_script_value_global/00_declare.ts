// Backlog 102, the headline case, value side: an ordinary `globals.d.ts`-shaped ambient value in
// a script file. `b102CrossValue` is a fresh name, so no library row is touched.
declare var b102CrossValue: number;

// Deliberately marker-free: this observes only that the user-owned incomplete in 00_augment.ts
// adds no demand-site diagnostic. A direct WU3 inspector owns terminal unavailable state and proof
// that no frozen-prefix/error/any surface was published.
const withheldBaseSurface: string = [1, 2].map((value) => value + 1);

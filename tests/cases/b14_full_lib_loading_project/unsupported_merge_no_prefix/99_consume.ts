// The demand-side incomplete proves the frozen Array prefix was not accepted after the merged
// surface became unavailable. A shipped direct inspector proves the terminal state.
const withheldBaseSurface: string = [1, 2].map((value) => value + 1); // incomplete[library-bridge/array/identity]
